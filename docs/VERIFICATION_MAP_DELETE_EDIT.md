# VERIFICATION — MAP 삭제/수정 + 연구소 제품 검증 체크리스트

> 대상 브랜치: `claude/request-form-page-update-0ucn8e`
> 범위: 요청 목적 `MAP 삭제/수정` + 기타 목적 `연구소 제품` 추가, 결재 경로(Case O),
> 함께 발견해 고친 버그 3건(결재현황 R 미표시·RA "대기" 오표시·후결자 제거 가드 누락),
> 마이그레이션 leaf 충돌 해소.
> 관련 문서: `docs/REQUEST.md` §4.1(2026-08-06 이력 2건), `docs/APPROVAL.md` Case O·§6-9,
> 설계 원본: `docs/map_delete_edit_mockup.html`(확정본 v2).
> ⚠️ 이 세션엔 Docker·브라우저가 없어 코드 레벨(tsc/test)까지만 실행했다. 아래 수동 시나리오는
> 실제 화면에서 **한 번도 확인되지 않았다** — 검증의 핵심은 이 문서다.

---

## 0. 사전 준비

```bash
cd /home/user/request-site
docker compose -f docker-compose.dev.yml up -d
docker ps --format "{{.Names}}"     # <backend>/<frontend> 컨테이너명 확인
```
- 접속: http://localhost:10011
- 필요한 개발용 로그인 역할: **의뢰자(NONE)** · **PL**(여러 명 — 후결자 지정용) · **TE_R** · **TE_P** ·
  **TE_J** · **TE_O**. `.env` 에 `POST_APPROVER_LOGINID`(TE_R 소속 1명)가 설정돼 있는지 먼저 확인
  (없으면 후결자 관련 시나리오 일부가 "고정 후결자 없음"으로 다르게 동작한다).

---

## 1. 자동 검증 (코드 레벨 — 이미 이 세션에서 실행 완료, 재확인용)

- [ ] 프론트 타입체크 — 신규 에러 0건(24개 유지)이어야 정상
  ```bash
  cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"
  ```
- [ ] 프론트 테스트 — 3 suites / 74건
  ```bash
  cd frontend && CI=true npx react-scripts test --watchAll=false --passWithNoTests
  ```
- [ ] 백엔드 마이그레이션 그래프 — 단일 체인, leaf 1개
  ```bash
  docker exec -it <backend> python manage.py showmigrations api
  docker exec -it <backend> python manage.py makemigrations --check --dry-run
  ```
- [ ] 백엔드 테스트 — 188건 중 185건 통과 정상(나머지 3건은 이 기능과 무관한 선행 버그:
  `ExternalApiKeyAccessTest.test_wrong_key_returns_401`,
  `MessageBuildingTest.test_broadcast_subject_has_no_name_prefix`,
  `HybridImmediateSendTest.test_enqueue_schedules_immediate_send_on_commit`)
  ```bash
  docker exec -it <backend> python manage.py test api
  ```

---

## 2. 연구소 제품 (Only MAP 전용 + 후결자 필수)

### 2-1. 노출 조건
- [ ] `/request` 진입 → STEP1 요청 목적에서 **`Only MAP`** 클릭
- [ ] 기타 목적 줄: 기존 6개(Layer 추가/삭제 ~ FirstA 변경)는 **회색 비활성**, **`연구소 제품`만 클릭 가능**
- [ ] 요청 목적을 `신규`/`차용`/`신규+차용`/`MAP 삭제/수정`/`기타` 로 바꿔보기 → 이번엔 반대로
  **`연구소 제품`만 회색**, 나머지 6개는 정상 선택 가능

### 2-2. 후결자 필수화
- [ ] `Only MAP` + `연구소 제품` 켠 채로 STEP5 까지 이동 → **상신** 클릭
  → 상신 모달에 **후결자 입력칸**이 나타나는지 확인
- [ ] 후결자를 지정하지 않고 상신 시도 → `"C가문 제품·연구소 제품은 후결자를 1명 이상 지정해주세요."`
  로 차단
- [ ] PL 후결자 1명 지정 후 상신 → 정상 상신됨

### 2-3. 해제 시 정리
- [ ] `연구소 제품` + 후결자 지정된 상태에서 요청 목적을 다른 값으로 변경 시도
  → **확인 모달**(`"다른 요청 목적으로 바꾸면 '연구소 제품' 선택과 지정한 후결자가 해제됩니다..."`)
  → 확인 → `연구소 제품` 선택 해제 + 후결자 목록 비워짐
- [ ] `Only MAP` 은 유지한 채 `연구소 제품`만 다시 꺼도 지정했던 후결자가 함께 초기화되는지 확인

### 2-4. 결재 경로는 변하지 않는다
- [ ] 연구소 제품 문서 상신 → PL 합의 → R 지정·합의 → 결재현황에서
  **`PL → R → 후결자`** 순서 그대로인지 확인(P/J/O/E 는 여전히 생성되지 않음 — 기존 Only MAP 과 동일)
- [ ] 후결자(고정 1명 + 지정 1명) 전원 합의 → `승인` 전환

### 2-5. 진행 중 후결자 제거 가드 (이번에 발견·수정한 버그)
- [ ] 위 문서가 `under_review` 상태일 때, 지정 후결자 1명(고정 제외 유일한 추가 후결자)을
  `remove-post-approver` 로 제거 시도(결재현황 상세 → 후결자 관리 UI)
  → **차단**되어야 함(`"C가문 제품·연구소 제품은 (고정 후결자 외) 후결자를 최소 1명 유지해야 합니다."`)
  → **실패 신호**: 제거가 성공하고 고정 후결자만 남는다면 회귀

---

## 3. MAP 삭제/수정 (P·R·J·O 병렬 경로)

### 3-1. 요청 목적 선택 시 Step1 잠금 (Only MAP과 동일 동작)
- [ ] STEP1 요청 목적에서 **`MAP 삭제/수정`** 클릭 → (이미 다른 값 선택돼 있었으면 확인 모달 → 확인)
- [ ] 기타 목적 6개 전부 **잠김**(연구소 제품도 잠김 — Only MAP 이 아니므로)
- [ ] 흐름도·Backbone·참조 요청서 블록도 잠기고 초기화됨

### 3-2. StepMap 버튼 6개
- [ ] STEP2 진입 → MAP 요청 목적 버튼이 **`신규(NEW)`/`차용(CLONE)`/`기등록(EXISTING)`/`ADI`/`수정`/`삭제`**
  6개 모두 표시
- [ ] `MAP 삭제/수정` 목적일 때: `수정`/`삭제`만 클릭 가능, 나머지 4개는 회색
- [ ] 다른 요청 목적일 때: 반대로 `수정`/`삭제` 2개가 회색(비활성)

### 3-3. 이유 입력칸 — 하위 블록 숨김 + 에디터
- [ ] `수정` 클릭 → **원본 위치·REV·Only C가문 제품·지도편차·예외구역·X표시(M-shot)·Inter·Map Option
  블록이 전부 사라지고**, "MAP 수정 이유" 라벨의 입력칸만 남는지 확인
- [ ] 입력칸에 텍스트 입력 + **이미지 붙여넣기(Ctrl+V)** → 이미지가 즉시 업로드되어 본문에 삽입되는지 확인
- [ ] 텍스트/이미지를 여러 개 넣어 내용을 길게 만들기 → **입력칸 자체(고정 높이)만 세로 스크롤**되고
  페이지 전체 높이는 늘어나지 않는지 확인
- [ ] 아무것도 입력하지 않고 '다음' 시도 → **필수 오류로 진행 차단**
- [ ] 이미지만 넣고(텍스트 없이) '다음' 시도 → **통과**되는지 확인(텍스트 없어도 이미지만으로 유효 판정)

### 3-4. 수정 ↔ 삭제 전환
- [ ] `수정` 선택 후 이유를 작성 → `삭제` 버튼 클릭
  → **라벨만 "MAP 삭제 이유"로 바뀌고 작성했던 내용은 그대로 남아있는지** 확인(확인 모달 없음)
- [ ] 다시 `수정`으로 되돌려도 내용 유지되는지 확인

### 3-5. 상신 및 결재 경로 — 4단계 병렬
- [ ] 이유 작성 후 상신 절차(지정 PL·검토자·통보처 지정)는 기존과 동일하게 진행 → 상신
- [ ] PL 합의 → 결재현황에서 **P·R·J·O 4개가 한꺼번에 병렬**로 생성되는지 확인
  (E·후결자(RA)는 생성되지 않음 — 결재현황에 해당 행 자체가 없어야 함)
- [ ] **P·J·O 를 먼저 합의**시키고 **R을 마지막에 합의** → 결재현황 목록에 R 이 남아있는 동안
  **"현재 단계"에 R(RFG) 행이 보이는지** 확인(이번에 고친 `path0`) → R 합의 시 `승인`으로 전환
- [ ] 반대로 **R을 먼저 합의**시키고 **P를 마지막에 합의** → 역시 정상적으로 `승인`까지 가는지 확인
  (일반 경로는 P 합의로 승인 판정을 하지 않으므로, 이 케이스가 이번에 새로 추가한 판정 분기의 핵심)
- [ ] R 담당자 지정 시 검토자(RV)까지 함께 지정 → 담당자만 합의한 상태에서는 아직 `승인` 안 됨,
  검토자까지 합의해야 `승인`
- [ ] P 담당자 검토중(claim) 선점 후 합의 시 검토자(PV, `reviewer_loginids`) 함께 지정 →
  검토자 합의 전까지 `승인` 안 됨

### 3-6. 결재 상세 화면
- [ ] 상신된 MAP 삭제/수정 문서 상세 → **MAP 정보 섹션**에 수정/삭제 이유 카드가 표시되는지
  (라벨이 `수정`/`삭제`에 맞게 나오는지, 이미지 포함 내용이 그대로 보이는지)
- [ ] **'결재 경로' 탭**에서 **후결자(RA) 행**이 `대기`가 아니라 **`해당없음`**으로 표시되는지 확인
  (승인 전/후 모두)
- [ ] E(MASK) 행도 `해당없음`으로 표시되는지 확인(J-layer 가 비어 있어 plel 판정 자체가 없음)

### 3-7. 반려
- [ ] P·R·J·O 중 아무 단계에서나 반려 → 문서 전체가 `rejected` 로 전환되는지 확인(기존 규칙과 동일)
- [ ] 재상신 시 새 회차로 PL 검토부터 다시 시작하는지 확인

---

## 4. 회귀 확인 (기존 경로가 그대로인지)

- [ ] **일반 문서**(`신규` 등) 상신 → PL 합의 → R 지정만 하고 아직 합의 전 상태에서 결재현황 확인
  → 이번 변경으로 새로 생긴 `path0`(R) 행이 **나타나지 않아야** 함(R 이 아직 병렬 진입 전이라
  기존 "R단계 순차" 표시 그대로여야 함)
- [ ] 일반 문서에서 R 합의 후 P/O 병렬 진입 → 이 시점에도 **R 행이 별도로 나타나지 않아야** 함
  (R 은 이미 완료 상태라 `path0` 조건에 안 걸림)
- [ ] **Only MAP** 문서(연구소 제품 없이) → 기존과 동일하게 `PL→R→후결자`, 결재현황·상세 모두
  변경 전과 동일하게 보이는지 확인
- [ ] **C가문**(`only_prodc=Yes`) 문서 → 후결자 필수·제거 가드 기존 규칙 그대로인지 확인
  (연구소 제품 관련 변경이 C가문 로직을 건드리지 않았는지)
- [ ] 기존 `ADI CD 변경` 목적 문서 작성 → StepMap 의 `ADI` 버튼 잠금 동작(표시 전용) 그대로인지 확인
  (map_type 버튼이 4개에서 6개로 늘어난 것이 ADI 로직에 영향 없는지)

---

## 5. 배포 시 필수

- [ ] 배포 전 `python manage.py migrate` 실행 확인(마이그레이션 재정렬 자체는 스키마 변경이
  없지만, 그래프가 바뀌었으므로 배포 파이프라인에서 마이그레이션 상태를 한 번 확인 권장)
- [ ] `.env` 에 `POST_APPROVER_LOGINID` 설정 여부 재확인(연구소 제품 시나리오가 이 값에 의존)

---

## 6. 변경 핵심 파일

- 프론트: `RequestPage/{index,constants}.tsx`, `RequestPage/components/{Step1,StepMap}.tsx`,
  `components/PagedDetailView.tsx`, `utils/approvalTable.ts`(+`approvalTable.test.ts` 신규),
  `types/index.ts`, `locales/{ko,en}.json`
- 백엔드: `api/models.py`, `api/views.py`, `api/mailer.py`, `api/tests.py`,
  `api/migrations/0010~0015_*.py`(재정렬)
- 문서: `docs/{REQUEST,APPROVAL,HOME_STATS}.md`, `docs/map_delete_edit_mockup.html`(설계 원본)

---

*검증 중 문제가 발견되면 이 문서 각 항목에 결과를 표시하고, 발견된 결함은 CLAUDE.md 규칙 K
형식(발견/위치/근거/영향/선택지)으로 별도 보고한다.*
